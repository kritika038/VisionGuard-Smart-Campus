[phases.setup]
nixPkgs = ["python313"]
aptPkgs = ["libx11-6", "libopenblas0", "liblapack3", "libgomp1"]

[phases.build]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
